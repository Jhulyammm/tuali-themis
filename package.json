{
  "name": "hack4her",
  "version": "0.0.1",
  "private": true,
  "description": "Always on Shelf - Tuali Hackathon 2026 - AI agent that learns web processes by demonstration",
  "scripts": {
    "dev:operator": "pnpm --filter operator-ui dev",
    "dev:portal": "pnpm --filter mock-portal dev -- --port 3001",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}
